// index.js

/**
 * 将两个数字相加
 * @param {number} a 第一个数字
 * @param {number} b 第二个数字
 * @returns {number} 两个数字的和
 */
function add(a, b) {
    return a + b;
  }
  
  // 导出 add 函数，使其可以在其他地方使用
  module.exports = add;
  